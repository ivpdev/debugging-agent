the history must contain:
- "get_source_code" tool is called
- "stdin_write" tool is called, with the output containing the phrase "Breakpoint 1:" 
- "stdin_write" tool is called, with the output containing the phrase "Process {some number} resuming"
- "stdin_write" tool is called, with the output containing the phrase "You guessed the number in 0 attempts!"