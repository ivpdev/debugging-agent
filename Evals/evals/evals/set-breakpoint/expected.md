the assistant should do following actions:
- uses "get_source_code" tool
- uses a tool to put a breakpoint to some line between 36 and 40 (inclusively).
- uses the tool "stdin_write" to run the program (command "run")
