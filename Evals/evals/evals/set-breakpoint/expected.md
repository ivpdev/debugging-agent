the assistant should do following actions:
- uses "get_source_code" tool
- uses the tool "stdin_write" to put a breakpoint to some line between 36 and 40 (inclusively). The breakpoint setting command should not contain file name.
- uses the tool "stdin_write" to run the program (command "run")
